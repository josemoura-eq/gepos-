/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { HealthAreaService } from "../healthArea.service";
import { HealthAreaCreateInput } from "./HealthAreaCreateInput";
import { HealthAreaWhereInput } from "./HealthAreaWhereInput";
import { HealthAreaWhereUniqueInput } from "./HealthAreaWhereUniqueInput";
import { HealthAreaFindManyArgs } from "./HealthAreaFindManyArgs";
import { HealthAreaUpdateInput } from "./HealthAreaUpdateInput";
import { HealthArea } from "./HealthArea";
import { ContributorFindManyArgs } from "../../contributor/base/ContributorFindManyArgs";
import { Contributor } from "../../contributor/base/Contributor";
import { ContributorWhereUniqueInput } from "../../contributor/base/ContributorWhereUniqueInput";
@swagger.ApiBearerAuth()
export class HealthAreaControllerBase {
  constructor(
    protected readonly service: HealthAreaService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: HealthArea })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: HealthAreaCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<HealthArea> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "HealthArea",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"HealthArea"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        healthfield: true,
        id: true,
        mainAreas: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [HealthArea] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(HealthAreaFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<HealthArea[]> {
    const args = plainToClass(HealthAreaFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "HealthArea",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        healthfield: true,
        id: true,
        mainAreas: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: HealthArea })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: HealthAreaWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<HealthArea | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "HealthArea",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        healthfield: true,
        id: true,
        mainAreas: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: HealthArea })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: HealthAreaWhereUniqueInput,
    @common.Body()
    data: HealthAreaUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<HealthArea | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "HealthArea",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"HealthArea"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          healthfield: true,
          id: true,
          mainAreas: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: HealthArea })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: HealthAreaWhereUniqueInput
  ): Promise<HealthArea | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          healthfield: true,
          id: true,
          mainAreas: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/contributors")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(ContributorFindManyArgs)
  async findManyContributors(
    @common.Req() request: Request,
    @common.Param() params: HealthAreaWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Contributor[]> {
    const query = plainToClass(ContributorFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Contributor",
    });
    const results = await this.service.findContributors(params.id, {
      ...query,
      select: {
        comune: true,
        contributorName: true,
        countryOfOrigin: true,
        createdAt: true,
        id: true,
        municipality: true,
        numberOfColaborators: true,
        organizationType: true,
        province: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/contributors")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "update",
    possession: "any",
  })
  async createContributors(
    @common.Param() params: HealthAreaWhereUniqueInput,
    @common.Body() body: HealthAreaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      contributors: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "HealthArea",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"HealthArea"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/contributors")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "update",
    possession: "any",
  })
  async updateContributors(
    @common.Param() params: HealthAreaWhereUniqueInput,
    @common.Body() body: ContributorWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      contributors: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "HealthArea",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"HealthArea"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/contributors")
  @nestAccessControl.UseRoles({
    resource: "HealthArea",
    action: "update",
    possession: "any",
  })
  async deleteContributors(
    @common.Param() params: HealthAreaWhereUniqueInput,
    @common.Body() body: HealthAreaWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      contributors: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "HealthArea",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"HealthArea"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
